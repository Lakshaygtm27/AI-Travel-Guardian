import { Bot, Send } from 'lucide-react'
import { useState } from 'react'

const answers = { 'best food near me?': 'Try Masala Chowk for a broad local-food sampler, then finish with ghewar from a traditional sweet shop. I can add either stop to your planner.' }
export default function Chatbot() { const [question, setQuestion] = useState(''); const [answer, setAnswer] = useState('Hello Alex. I can help you organize trip details, reservations, and questions for your next journey.'); const ask = (event) => { event.preventDefault(); const normalized = question.trim().toLowerCase(); setAnswer(answers[normalized] || 'I have saved that question for your travel desk. Try “Best food near me?” for a local Jaipur recommendation.'); setQuestion('') }; return <><div className="page-heading"><div><p className="eyebrow">Your travel desk</p><h1>Trip companion</h1><p className="page-subtitle">A calm place to collect questions for your itinerary.</p></div></div><section className="panel chat-panel"><div><div className="panel-header"><h2><Bot size={18} /> Travel desk</h2><span className="status">Online</span></div><div className="chat-message">{answer}</div></div><form className="chat-input" onSubmit={ask}><input aria-label="Message" value={question} onChange={(event) => setQuestion(event.target.value)} placeholder="Best food near me?" /><button className="primary-button" type="submit" aria-label="Send message"><Send size={16} /></button></form></section></> }
